package org.swetlokognatsk.earking_out.core.domain.model.exercises;

// TODO why menu is always random? i saw `enum.values()` guarantee to always return elements in the same order as they are specified
public enum ExerciseNames {
    PERFECT_PITCH, MELODIC_INTERVALS, HARMONIC_INTERVALS,
    KEYS,
}
